# 🚀 دليل النشر على Streamlit Cloud

هذا الدليل يشرح كيفية نشر التطبيق على Streamlit Cloud.

---

## 📋 المتطلبات

1. ✅ حساب على [GitHub](https://github.com)
2. ✅ حساب على [Streamlit Cloud](https://streamlit.io/cloud)
3. ✅ حساب على [Google Cloud Console](https://console.cloud.google.com/)
4. ✅ Service Account من Google Cloud Console (مجاني 100%)

---

## 🔧 الخطوة 1: إعداد GitHub Repository

### 1.1 رفع الكود على GitHub

```bash
# إذا لم يكن لديك repository بعد
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

**ملاحظة مهمة:** تأكد من رفع ملف `Amiri-Bold.ttf` على GitHub (لا يجب أن يكون في `.gitignore`).

---

## 🔑 الخطوة 2: إعداد OAuth Token

### 2.1 إنشاء OAuth Client ID (5 دقائق - مجاني 100%)

**⚠️ يجب إكمال هذه الخطوة محلياً على جهازك أولاً**

1. افتح [Google Cloud Console](https://console.cloud.google.com/)
2. اذهب إلى **APIs & Services** → **Credentials**
3. اضغط **+ CREATE CREDENTIALS** → **OAuth client ID**
4. **Application type**: اختر **Desktop app**
5. **Name**: `Certificate Generator Desktop`
6. اضغط **CREATE**
7. اضغط **Download JSON** (⬇️) لتنزيل الملف
8. سمّ الملف: `oauth_credentials.json`
9. ضعه في مجلد المشروع

### 2.2 تفعيل Google Drive API

1. اذهب إلى **APIs & Services** → **Library**
2. ابحث عن: **Google Drive API**
3. اضغط **ENABLE** (إذا لم يكن مفعّلاً)

### 2.3 توليد OAuth Token محلياً

**📌 هذه الخطوة تُنفّذ على جهازك المحلي فقط:**

1. افتح Terminal/Command Prompt
2. اذهب لمجلد المشروع:
   ```bash
   cd path/to/cert_gen
   ```
3. شغّل سكريبت توليد Token:
   ```bash
   python generate_token.py
   ```
4. سيفتح المتصفح - سجّل دخول بحسابك على Google
5. اضغط **السماح** (Allow)
6. سيُنشأ ملف `token.json` تلقائياً

**💡 للمزيد من التفاصيل:** راجع ملف `TOKEN_SETUP.md`

---

## ☁️ الخطوة 3: إعداد Streamlit Cloud

### 3.1 ربط GitHub مع Streamlit Cloud

1. افتح [Streamlit Cloud](https://share.streamlit.io/)
2. اضغط **Sign in** وسجّل دخول بحساب GitHub
3. اضغط **New app**
4. اختر:
   - **Repository**: repository الخاص بك
   - **Branch**: `main` (أو `master`)
   - **Main file path**: `app.py`

### 3.2 إضافة OAuth Token إلى Streamlit Secrets

1. في صفحة التطبيق، اضغط **☰** (القائمة) → **Settings**
2. اذهب إلى **Secrets**
3. افتح ملف `token.json` الذي أنشأته محلياً
4. انسخ **محتوى الملف بالكامل**
5. أضف المحتوى التالي في Streamlit Secrets:

```toml
[google_drive_token]
token = '''
{
  "token": "ya29.a0AfB_byB...",
  "refresh_token": "1//0g...",
  "token_uri": "https://oauth2.googleapis.com/token",
  "client_id": "123456789-xxx.apps.googleusercontent.com",
  "client_secret": "GOCSPX-xxx",
  "scopes": ["https://www.googleapis.com/auth/drive.file"],
  "expiry": "2024-01-01T12:00:00.000000Z"
}
'''
```

**⚠️ ملاحظات مهمة:**
- ✅ استبدل المحتوى داخل `'''` بمحتوى `token.json` الخاص بك **بالكامل**
- ✅ احتفظ بـ `[google_drive_token]` و `token = '''` كما هي
- ✅ استخدم `'''` **ثلاث** علامات تنصيص في البداية والنهاية
- ✅ لا تعدل أي شيء في محتوى JSON - انسخه كما هو
- 🔄 بعد الحفظ، اضغط **Save** ثم أعد تشغيل التطبيق من **Manage app** → **Reboot**

**مثال كامل:**
```toml
[google_drive_token]
token = '''{"token": "ya29...", "refresh_token": "1//0g...", "token_uri": "https://oauth2.googleapis.com/token", "client_id": "123-xxx.apps.googleusercontent.com", "client_secret": "GOCSPX-xxx", "scopes": ["https://www.googleapis.com/auth/drive.file"], "expiry": "2024-01-01T12:00:00Z"}'''
```

### 3.3 بدء النشر

1. اضغط **Save**
2. اضغط **Deploy** أو انتظر النشر التلقائي
3. بعد النشر، ستحصل على رابط مثل: `https://YOUR_APP_NAME.streamlit.app/`

### 3.4 التحقق من نجاح الإعداد

بعد النشر، افتح التطبيق وتحقق من:
- ✅ يظهر في الـ sidebar: "✅ متصل بالدرايف"
- ✅ يمكنك إدخال اسم المجلد
- ✅ يمكنك رفع ملف تجريبي
- ✅ الملفات تظهر في Google Drive الخاص بك
- ❌ إذا ظهر "❌ OAuth Token غير موجود" → راجع قسم **حل المشاكل** أدناه
- ⚠️ إذا ظهر خطأ آخر → راجع `TOKEN_SETUP.md` للتفاصيل

---

## ✅ الخطوة 4: التحقق من النجاح

بعد إضافة Token في Secrets وإعادة التشغيل:

1. افتح التطبيق على Streamlit Cloud
2. يجب أن تظهر في sidebar: **✅ متصل بالدرايف**
3. جرّب رفع ملف تجريبي
4. افتح [Google Drive](https://drive.google.com/)
5. يجب أن تجد المجلد والملفات في حسابك مباشرة

**💡 الملفات ستظهر في حساب Google Drive الذي استخدمته عند توليد Token**

---

## ⚠️ ملاحظات مهمة

### 1. ملف الخط العربي
- ✅ تأكد من رفع `Amiri-Bold.ttf` على GitHub
- ✅ الملف يجب أن يكون في نفس المجلد مع `app.py`

### 2. OAuth Token
- 🔒 **أمان:** لا ترفع ملف `token.json` على GitHub أبداً
- ✅ الملف محمي في `.gitignore`
- ✅ استخدم Streamlit Secrets فقط للنشر
- 🔄 Token يُجدّد تلقائياً عند انتهاء صلاحيته

### 3. الملفات في حسابك
- 📁 الملفات تُرفع في حساب Google Drive الذي استخدمته للتوليد
- ✅ لا حاجة لمشاركة المجلدات
- ✅ تجد الملفات مباشرة في Drive الخاص بك

### 4. الحدود المجانية
- ✅ 1 مليار استعلام/يوم مجاناً
- ✅ 15 GB تخزين مجاني (من حساب Google Drive الخاص بك)
- ✅ لا يحتاج بطاقة ائتمان

---

## 🧪 اختبار التطبيق

بعد النشر:

1. افتح رابط التطبيق: `https://YOUR_APP_NAME.streamlit.app/`
2. جرّب رفع ملف CSV/Excel و PDF
3. تأكد من عمل المصادقة مع Google Drive
4. تأكد من رفع الشهادات بنجاح

---

## 🔧 حل المشاكل

### المشكلة: "OAuth Token غير موجود"
**الحل:** تأكد من إضافة Token في Streamlit Cloud Settings

**خطوات التحقق:**
1. تأكد أنك ولّدت token محلياً: `python generate_token.py`
2. افتح تطبيقك على Streamlit Cloud
3. اضغط **☰** → **Settings** → **Secrets**
4. تأكد من وجود `[google_drive_token]` مع محتوى `token.json`
5. اضغط **Save**
6. من **Manage app** → اضغط **Reboot**

### المشكلة: "could not locate runnable browser"
**الحل:**
- ✅ هذا طبيعي - `generate_token.py` يجب أن يُشغّل **محلياً على جهازك**
- ❌ لا تحاول تشغيله على Streamlit Cloud
- 💡 ولّد token على جهازك ثم انسخه إلى Secrets

### المشكلة: "Service Accounts do not have storage quota"
**الحل:**
- ✅ استخدم OAuth Token بدلاً من Service Account
- ✅ اتبع الخطوات في `TOKEN_SETUP.md`

### المشكلة: "Invalid token" أو "Token expired"
**الحل:**
1. ولّد token جديد: `python generate_token.py`
2. انسخ محتوى `token.json` الجديد إلى Secrets
3. أعد تشغيل التطبيق

### المشكلة: "Permission denied" عند الرفع
**الحل:**
1. تأكد أن Google Drive API مفعّلة
2. تأكد من الموافقة على الصلاحيات عند توليد Token
3. جرّب توليد token جديد

### المشكلة: "SSL: WRONG_VERSION_NUMBER"
**الحل:** 
- تأكد من اتصالك بالإنترنت
- حاول إعادة تحميل الصفحة
- إذا كنت خلف Proxy، قد تحتاج لإعدادات إضافية

---

## 📚 ملفات مرجعية

- `README.md` - الدليل الشامل
- `TOKEN_SETUP.md` - **دليل توليد OAuth Token (مهم!)**
- `QUICKSTART.md` - البدء السريع
- `SETUP_INSTRUCTIONS.md` - إعداد OAuth
- `TROUBLESHOOTING.md` - حل المشاكل

---

## ✅ Checklist قبل النشر

- [ ] الكود مرفوع على GitHub
- [ ] ملف `Amiri-Bold.ttf` موجود في Repository
- [ ] OAuth Client ID تم إنشاؤه (Desktop app)
- [ ] Google Drive API مفعّلة
- [ ] تم توليد `token.json` محلياً (`python generate_token.py`)
- [ ] محتوى `token.json` مضاف في Streamlit Secrets
- [ ] الصيغة صحيحة: `[google_drive_token]` مع `token = '''...'''`
- [ ] تم إعادة تشغيل التطبيق (Reboot)
- [ ] يظهر "✅ متصل بالدرايف" في sidebar
- [ ] تم اختبار رفع ملف والتأكد من ظهوره في Google Drive

---

**بعد إكمال جميع الخطوات، التطبيق سيكون جاهزاً على Streamlit Cloud! 🎉**

