# 🚀 دليل النشر على Streamlit Cloud

هذا الدليل يشرح كيفية نشر التطبيق على Streamlit Cloud.

---

## 📋 المتطلبات

1. ✅ حساب على [GitHub](https://github.com)
2. ✅ حساب على [Streamlit Cloud](https://streamlit.io/cloud)
3. ✅ حساب على [Google Cloud Console](https://console.cloud.google.com/)
4. ✅ Service Account من Google Cloud Console (مجاني 100%)

---

## 🔧 الخطوة 1: إعداد GitHub Repository

### 1.1 رفع الكود على GitHub

```bash
# إذا لم يكن لديك repository بعد
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

**ملاحظة مهمة:** تأكد من رفع ملف `Amiri-Bold.ttf` على GitHub (لا يجب أن يكون في `.gitignore`).

---

## 🔑 الخطوة 2: إعداد Service Account

### 2.1 إنشاء Service Account (5 دقائق - مجاني 100%)

1. افتح [Google Cloud Console](https://console.cloud.google.com/)
2. اذهب إلى **IAM & Admin** → **Service Accounts**
3. اضغط **+ CREATE SERVICE ACCOUNT**
4. املأ البيانات:
   ```
   Service account name: certificate-uploader
   Service account ID: (سيُملأ تلقائياً)
   Description: Upload certificates to Drive
   ```
5. اضغط **CREATE AND CONTINUE**
6. في **Grant this service account access**:
   - اتركها فارغة أو اختر "Basic" → "Viewer" (اختياري)
   - اضغط **CONTINUE**
7. في **Grant users access**:
   - اتركها فارغة
   - اضغط **DONE**

### 2.2 تفعيل Google Drive API

1. اذهب إلى **APIs & Services** → **Library**
2. ابحث عن: **Google Drive API**
3. اضغط **ENABLE** (إذا لم يكن مفعّلاً)

### 2.3 إنشاء مفتاح Service Account

1. في صفحة **Service Accounts**، اضغط على Service Account الذي أنشأته
2. اذهب إلى تبويب **KEYS**
3. اضغط **ADD KEY** → **Create new key**
4. اختر **JSON**
5. اضغط **CREATE**
6. سيتم تنزيل ملف JSON تلقائياً - احفظه بأمان!

**📌 ملاحظة:** هذا الملف يحتوي على مفتاح خاص - لا تشاركه مع أحد!

---

## ☁️ الخطوة 3: إعداد Streamlit Cloud

### 3.1 ربط GitHub مع Streamlit Cloud

1. افتح [Streamlit Cloud](https://share.streamlit.io/)
2. اضغط **Sign in** وسجّل دخول بحساب GitHub
3. اضغط **New app**
4. اختر:
   - **Repository**: repository الخاص بك
   - **Branch**: `main` (أو `master`)
   - **Main file path**: `app.py`

### 3.2 إضافة Service Account إلى Streamlit Secrets

1. في صفحة التطبيق، اضغط **☰** (القائمة) → **Settings**
2. اذهب إلى **Secrets**
3. افتح ملف JSON الذي نزّلته من Google Cloud Console
4. انسخ **محتوى الملف بالكامل**
5. أضف المحتوى التالي في Streamlit Secrets:

```toml
[service_account]
type = "service_account"
project_id = "YOUR_PROJECT_ID"
private_key_id = "YOUR_PRIVATE_KEY_ID"
private_key = """YOUR_PRIVATE_KEY_HERE"""
client_email = "YOUR_SERVICE_ACCOUNT_EMAIL"
client_id = "YOUR_CLIENT_ID"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "YOUR_CERT_URL"
universe_domain = "googleapis.com"
```

**⚠️ ملاحظات مهمة:**
- ✅ استبدل كل `YOUR_*` بالقيم الفعلية من ملف JSON المُنزّل
- ✅ `private_key` يجب أن يكون داخل `"""` الثلاثية (يحتوي على أسطر متعددة)
- ✅ احتفظ بالـ `\n` في `private_key` كما هي
- ✅ **مثال للـ private_key:**
  ```toml
  private_key = """-----BEGIN PRIVATE KEY-----
MIIEvQIBADANBgk...
...المحتوى هنا...
...أسطر متعددة...
-----END PRIVATE KEY-----
"""
  ```
- 🔄 بعد الحفظ، اضغط **Save** ثم أعد تشغيل التطبيق من **Manage app** → **Reboot**

### 3.3 بدء النشر

1. اضغط **Save**
2. اضغط **Deploy** أو انتظر النشر التلقائي
3. بعد النشر، ستحصل على رابط مثل: `https://YOUR_APP_NAME.streamlit.app/`

### 3.4 التحقق من نجاح الإعداد

بعد النشر، افتح التطبيق وتحقق من:
- ✅ يظهر في الـ sidebar: "✅ متصل بالدرايف"
- ✅ يمكنك إدخال اسم المجلد
- ✅ يمكنك إدخال بريدك الإلكتروني
- ❌ إذا ظهر "❌ ملف Service Account غير موجود" → راجع قسم **حل المشاكل** أدناه
- ⚠️ إذا ظهر "⚠️ خطأ في قراءة Secrets" → تحقق من صيغة Secrets (خصوصاً private_key)

---

## 📧 الخطوة 4: مشاركة المجلد (مهمة!)

لأن Service Account يرفع الملفات في "حسابه" الخاص، يجب مشاركة المجلد معك:

### الطريقة التلقائية (الأسهل):
1. في التطبيق، أدخل بريدك الإلكتروني في حقل "📧 بريدك الإلكتروني"
2. عند إنشاء/فتح المجلد، سيتم مشاركته معك تلقائياً
3. ستصلك دعوة على البريد الإلكتروني
4. افتح البريد واقبل الدعوة

### الطريقة اليدوية:
1. افتح [Google Drive](https://drive.google.com/)
2. ابحث عن المجلد (مثلاً "شهادات الكورس")
3. انقر بزر الماوس الأيمن → **مشاركة**
4. أضف Service Account email من ملف JSON:
   ```
   example-service@your-project.iam.gserviceaccount.com
   ```
5. اختر صلاحيات "محرر" (Editor)
6. اضغط **إرسال**

---

## ⚠️ ملاحظات مهمة

### 1. ملف الخط العربي
- ✅ تأكد من رفع `Amiri-Bold.ttf` على GitHub
- ✅ الملف يجب أن يكون في نفس المجلد مع `app.py`

### 2. Service Account Credentials
- 🔒 **أمان:** لا ترفع ملف `client_secrets.json` على GitHub أبداً
- ✅ الملف محمي في `.gitignore`
- ✅ استخدم Streamlit Secrets فقط للنشر

### 3. مشاركة المجلدات
- 📧 **مهم:** أدخل بريدك في التطبيق لمشاركة المجلد تلقائياً
- 🔄 المشاركة تتم مرة واحدة فقط
- ✅ بعدها ستجد جميع الملفات في Google Drive الخاص بك

### 4. الحدود المجانية
- ✅ 1 مليار استعلام/يوم مجاناً
- ✅ 15 GB تخزين مجاني
- ✅ لا يحتاج بطاقة ائتمان

---

## 🧪 اختبار التطبيق

بعد النشر:

1. افتح رابط التطبيق: `https://YOUR_APP_NAME.streamlit.app/`
2. جرّب رفع ملف CSV/Excel و PDF
3. تأكد من عمل المصادقة مع Google Drive
4. تأكد من رفع الشهادات بنجاح

---

## 🔧 حل المشاكل

### المشكلة: "ملف Service Account غير موجود"
**الحل:** تأكد من إضافة Secrets في Streamlit Cloud Settings

**خطوات التحقق:**
1. افتح تطبيقك على Streamlit Cloud
2. اضغط **☰** → **Settings** → **Secrets**
3. تأكد من وجود `[service_account]` مع جميع الحقول المطلوبة
4. اضغط **Save**
5. من **Manage app** → اضغط **Reboot** لإعادة تشغيل التطبيق

### المشكلة: "⚠️ خطأ في قراءة Secrets"
**الحل:** 
1. تأكد من نسخ محتوى ملف JSON كاملاً
2. تأكد أن `private_key` داخل `"""` الثلاثية
3. لا تحذف أي `\n` من `private_key`
4. راجع القسم 3.2 للصيغة الصحيحة

### المشكلة: "لا أرى الملفات في Google Drive"
**الحل:**
1. أدخل بريدك الإلكتروني في حقل "📧 بريدك الإلكتروني" بالتطبيق
2. أو شارك المجلد يدوياً مع نفسك (راجع الخطوة 4)
3. تحقق من البريد الوارد لقبول دعوة المشاركة

### المشكلة: "Permission denied" عند الرفع
**الحل:**
1. تأكد أن Google Drive API مفعّلة
2. تأكد أن Service Account لديه صلاحيات على المجلد
3. جرب حذف المجلد وإنشاؤه من جديد عبر التطبيق

### المشكلة: "SSL: WRONG_VERSION_NUMBER"
**الحل:** 
- تأكد من اتصالك بالإنترنت
- حاول إعادة تحميل الصفحة
- إذا كنت خلف Proxy، قد تحتاج لإعدادات إضافية

---

## 📚 ملفات مرجعية

- `README.md` - الدليل الشامل
- `QUICKSTART.md` - البدء السريع
- `SETUP_INSTRUCTIONS.md` - إعداد OAuth
- `TROUBLESHOOTING.md` - حل المشاكل

---

## ✅ Checklist قبل النشر

- [ ] الكود مرفوع على GitHub
- [ ] ملف `Amiri-Bold.ttf` موجود في Repository
- [ ] Service Account تم إنشاؤه في Google Cloud Console
- [ ] Google Drive API مفعّلة
- [ ] ملف Service Account JSON تم تنزيله
- [ ] Streamlit Secrets مُعدّ بشكل صحيح (بجميع حقول service_account)
- [ ] بريدك الإلكتروني مضاف في التطبيق لمشاركة المجلد
- [ ] التطبيق يعمل محلياً بدون أخطاء

---

**بعد إكمال جميع الخطوات، التطبيق سيكون جاهزاً على Streamlit Cloud! 🎉**

