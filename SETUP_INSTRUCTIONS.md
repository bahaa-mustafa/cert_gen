# 📋 تعليمات الإعداد - خطوة بخطوة

## ✅ ما تم إنجازه

تم تحويل التطبيق بنجاح من **Service Account** إلى **OAuth 2.0**!

### التغييرات الرئيسية:
- ✅ نظام مصادقة OAuth جديد (تسجيل دخول مرة واحدة)
- ✅ إنشاء المجلدات تلقائياً في Drive
- ✅ رفع الملفات في حساب المستخدم مباشرة
- ✅ لا قيود على storage quota
- ✅ واجهة محدّثة ورسائل أوضح

---

## 🎯 ما المطلوب منك الآن؟

### الخطوة 1: إنشاء OAuth Credentials (5-10 دقائق)

#### أ) افتح Google Cloud Console
🔗 [https://console.cloud.google.com/](https://console.cloud.google.com/)

#### ب) اختر/أنشئ المشروع
- إذا كان لديك مشروع `drive-certificate` → اختره
- أو أنشئ مشروعاً جديداً

#### ج) تفعيل Google Drive API
1. من القائمة: **APIs & Services** → **Library**
2. ابحث عن: **Google Drive API**
3. اضغط: **Enable** (إذا لم يكن مفعّلاً)

#### د) إعداد OAuth Consent Screen
1. **APIs & Services** → **OAuth consent screen**
2. اختر: **External** → **Create**
3. املأ المطلوب:
   ```
   App name: Certificate Generator
   User support email: [إيميلك]
   Developer contact: [إيميلك]
   ```
4. **Save and Continue**
5. في صفحة **Scopes**:
   - **Add or Remove Scopes**
   - ابحث وحدد: `https://www.googleapis.com/auth/drive.file`
   - **Update** → **Save and Continue**
6. **Save and Continue** مرتين → **Back to Dashboard**

#### هـ) إنشاء OAuth Client ID
1. **APIs & Services** → **Credentials**
2. **+ Create Credentials** → **OAuth client ID**
3. Application type: **Desktop app**
4. Name: `Certificate Generator Desktop`
5. **Create**

#### و) تنزيل الملف
1. اضغط زر **Download JSON** (⬇️)
2. **غيّر اسم الملف إلى:** `oauth_credentials.json`
3. **انقل الملف إلى مجلد التطبيق** (نفس مكان `app.py`)

---

### الخطوة 2: تشغيل التطبيق

```bash
# تأكد من تثبيت المكتبات الجديدة
pip install -r requirements.txt

# شغّل التطبيق
streamlit run app.py
```

---

### الخطوة 3: تسجيل الدخول (مرة واحدة فقط!)

عند تشغيل التطبيق لأول مرة:

1. **المتصفح سيفتح تلقائياً** → صفحة تسجيل دخول Google
2. **سجّل دخول** بحسابك الشخصي على Google
3. **اضغط "Allow" أو "السماح"** لمنح التطبيق صلاحية الوصول
4. **ارجع لتطبيق Streamlit** → سترى رسالة "✅ تم الاتصال بنجاح"

**هذا كل شيء!** 🎉

في المرات القادمة، التطبيق سيعمل مباشرة بدون طلب تسجيل دخول.

---

## 🚀 الاستخدام

الآن يمكنك:

1. **رفع ملف الأسماء** (CSV/Excel)
2. **رفع صورة الشهادة** (JPG/PNG)
3. **ضبط الإحداثيات** (مكان الاسم)
4. **كتابة اسم المجلد** (سيُنشأ تلقائياً إذا لم يكن موجوداً)
5. **اضغط "بدء التوليد والرفع"** 🚀

---

## ❓ الأسئلة الشائعة

### س: أين ستُحفظ الشهادات؟
**ج:** في حساب Google Drive الخاص بك، في المجلد الذي تحدده.

### س: هل يمكن للعملاء استخدام التطبيق؟
**ج:** نعم، بطريقتين:
1. **حالياً:** أنت تشغل التطبيق وتولد الشهادات للعملاء
2. **لاحقاً:** يمكن استضافته على سيرفر وكل عميل يسجل دخول بحسابه

### س: ماذا لو حذفت `token.json`؟
**ج:** سيُطلب منك تسجيل الدخول مرة أخرى فقط.

### س: هل ملف `client_secrets.json` القديم لازم؟
**ج:** لا، لم يعد مطلوباً. يمكنك حذفه أو الاحتفاظ به.

### س: هل التطبيق آمن؟
**ج:** نعم!
- يستخدم OAuth الرسمي من Google
- يطلب صلاحية `drive.file` فقط (الملفات التي ينشئها فقط)
- لا يصل لملفاتك الأخرى في Drive

---

## 🔒 ملاحظات أمان مهمة

⚠️ **لا تشارك هذه الملفات مع أي شخص:**
- `oauth_credentials.json`
- `token.json`

✅ **هذه الملفات محمية في `.gitignore`** - لن تُرفع على GitHub

---

## 📚 ملفات التوثيق

- **`QUICKSTART.md`** → دليل البدء السريع
- **`README.md`** → الدليل الشامل
- **`CHANGELOG.md`** → سجل التغييرات

---

## 💡 نصيحة أخيرة

جرّب التطبيق بملف صغير (3-5 أسماء) أولاً لتختبر:
- صلاحية الاتصال
- ضبط إحداثيات الاسم على الشهادة
- سرعة الرفع

---

**جاهز؟ ابدأ الآن! 🎉**

```bash
streamlit run app.py
```

إذا واجهت أي مشكلة، راجع:
- `README.md` → قسم استكشاف الأخطاء
- أو اتصل بالدعم الفني

