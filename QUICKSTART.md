# 🚀 دليل البدء السريع

## الخطوات المطلوبة منك (لمرة واحدة فقط)

### 1️⃣ إعداد OAuth Credentials على Google Cloud Console

#### أ) إنشاء/اختيار مشروع
1. افتح [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروعاً جديداً أو اختر مشروعاً موجوداً

#### ب) تفعيل Google Drive API
1. من القائمة: **APIs & Services** → **Library**
2. ابحث عن **Google Drive API** → اضغط **Enable**

#### ج) إعداد OAuth Consent Screen
1. **APIs & Services** → **OAuth consent screen**
2. اختر **External** → **Create**
3. املأ:
   - App name: `Certificate Generator`
   - User support email: (إيميلك)
   - Developer contact: (إيميلك)
4. **Save and Continue**
5. في **Scopes** → **Add or Remove Scopes**:
   - حدد: `https://www.googleapis.com/auth/drive.file`
   - **Update** → **Save and Continue**
6. **Save and Continue** → **Back to Dashboard**

#### د) إنشاء OAuth Client ID
1. **APIs & Services** → **Credentials**
2. **Create Credentials** → **OAuth client ID**
3. Application type: **Desktop app**
4. Name: `Certificate Generator Desktop`
5. **Create**
6. **Download JSON** (⬇️ زرار التنزيل)
7. **غيّر اسم الملف إلى: `oauth_credentials.json`**
8. **ضع الملف في مجلد التطبيق** (نفس مكان `app.py`)

---

### 2️⃣ تشغيل التطبيق

```bash
# تثبيت المكتبات (مرة واحدة فقط)
pip install -r requirements.txt

# تشغيل التطبيق
streamlit run app.py
```

---

### 3️⃣ أول استخدام (تسجيل الدخول)

عند التشغيل لأول مرة:

1. **المتصفح سيفتح تلقائياً** بصفحة تسجيل دخول Google
2. **سجّل دخول بحسابك الشخصي**
3. **اضغط "Allow/السماح"**
4. **ارجع للتطبيق** - الاتصال تم بنجاح! ✅

**في المرات القادمة:** لن تحتاج لتسجيل دخول مجدداً 🎉

---

### 4️⃣ استخدام التطبيق

1. **ارفع ملف الأسماء** (CSV أو Excel - أول عمود = الأسماء)
2. **ارفع صورة الشهادة** (JPG/PNG)
3. **اضبط الإحداثيات** (مكان الاسم على الشهادة)
4. **حدد اسم المجلد** في Drive (سيُنشأ تلقائياً إذا لم يكن موجوداً)
5. **اضغط "بدء التوليد والرفع"** 🚀

---

## ❓ استكشاف الأخطاء الشائعة

### "ملف OAuth غير موجود"
✅ **الحل:** تأكد من وجود `oauth_credentials.json` في مجلد التطبيق

### "خطأ في المصادقة"
✅ **الحل:** 
```bash
# احذف الملف واعد تسجيل الدخول
rm token.json
streamlit run app.py
```

### "فشل إنشاء المجلد"
✅ **الحل:** تأكد من اتصالك بالإنترنت وأنك سمحت بالصلاحيات

---

## 📁 الملفات المطلوبة

```
cert_gen/
├── app.py                      ✅ موجود
├── requirements.txt            ✅ موجود
├── Amiri-Bold.ttf             ✅ موجود
├── oauth_credentials.json      ⚠️ يجب تنزيله من Google Cloud
└── token.json                  ✅ يُنشأ تلقائياً بعد التسجيل
```

---

## 🎯 ملاحظات مهمة

- **لا تشارك** `oauth_credentials.json` أو `token.json` مع أحد
- **الشهادات ستُرفع** في حساب Google Drive الخاص بك
- **المجلد يُنشأ تلقائياً** - لا حاجة لإنشائه يدوياً
- **الخط العربي** `Amiri-Bold.ttf` يجب أن يكون موجوداً

---

## 💡 نصائح

- جرّب بملف صغير أولاً (3-5 أسماء) لتختبر الإحداثيات
- استخدم صور بحجم A4 أفقي (842×595 pixels) للحصول على أفضل النتائج
- اضبط قيم X و Y تدريجياً حتى يظهر الاسم في المكان المناسب

---

**جاهز للبدء؟ شغّل التطبيق الآن! 🚀**

```bash
streamlit run app.py
```

